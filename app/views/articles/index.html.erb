<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and cunting!

<ul>
    <% @articles.each do |article| %>
      <% if article.archived? == false %>
        <li>
          <%= link_to article.title, article %>
        </li>
      <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %> 